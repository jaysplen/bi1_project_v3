pandas>=2.0,<3
numpy>=1.24,<3
scikit-learn>=1.3,<2
mlxtend>=0.22,<1
matplotlib>=3.7,<4
jupyter>=1.0,<2

# PDF build (markdown -> HTML; LibreOffice required for HTML -> PDF)
markdown>=3.5,<4

# Presentation deck (PPTX -> PDF via LibreOffice)
python-pptx>=1.0,<2

# Optional but useful for headless notebook execution
ipykernel>=6,<7
pytest>=7,<10
